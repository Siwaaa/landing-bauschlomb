.nav {
  background-color: var(--color-blue);

  &__content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 100px;
  }

  &__logo {
    max-width: 220px;

    & img {
      width: 100%;
      height: 100%;
      object-fit: contain;
    }
  }
}